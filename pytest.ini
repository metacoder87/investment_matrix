[pytest]
testpaths =
    tests
norecursedirs =
    .git
    .venv
    .tmp_pytest
    pytest-cache-files-*
markers =
    integration: boots the Docker Compose stack and validates live services end to end
